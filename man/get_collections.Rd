% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_collections.R
\name{get_collections}
\alias{get_collections}
\title{Generate collection event keys from taxon name, date, and coordinates}
\usage{
get_collections(occ_import = NA, taxa_checked = NA, precision = 2L)
}
\arguments{
\item{occ_import}{An \code{import} object returned by \code{\link[=import_records]{import_records()}}.}

\item{taxa_checked}{A \code{occ_taxa} object returned by
\code{\link[=check_taxon]{check_taxon()}}, used to supply the accepted taxon name for each
record.}

\item{precision}{Integer number of decimal places used to round latitude and
longitude when constructing the spatial portion of the key. This indirectly
controls the spatial tolerance for treating two records as the same
collection event. Typical choices:
\itemize{
\item \code{1}: ~10 km
\item \code{2}: ~1 km (default)
\item \code{3}: ~100 m
\item \code{4}: ~10 m
}

The default of \code{2} balances the fact that GBIF coordinates are often
recorded to 5 decimal places (~1 m) whereas the accompanying
\code{coordinateUncertaintyInMeters} is commonly around 500 m, making
sub-100 m grouping unreliable without additional filtering.}
}
\value{
A list of class \code{"collections"} with four elements:
\itemize{
\item \code{occ_key}: a \code{data.table} of the occurrence records with an added
\code{collection_key} column.
\item \code{complete_keys}: the number of distinct complete keys.
\item \code{incomplete_keys}: the number of distinct incomplete keys (those with at
least one \code{NA} component).
\item \code{runtime}: the elapsed execution time.
}
}
\description{
Builds a composite key for each occurrence record by combining the
accepted taxon name from \code{\link[=check_taxon]{check_taxon()}}, the collection date, and the rounded
geographic coordinates. Records sharing the same key are likely to represent
the same collection event and can be treated as duplicates.
}
\details{
\subsection{Key construction}{

The collection key is a pipe-delimited string with four components:

\if{html}{\out{<div class="sourceCode">}}\preformatted{wcvp_taxon_name | eventDate_numeric | rounded_latitude | rounded_longitude
}\if{html}{\out{</div>}}
\itemize{
\item \code{wcvp_taxon_name}: the accepted taxon name from \code{taxa_checked}.
\item \code{eventDate_numeric}: the \code{eventDate} field with time-of-day and ISO
separators removed, parsed as a date with \code{\link[lubridate:parse_date_time]{lubridate::parse_date_time()}}
(accepting truncated \code{ymd} orders), and converted to a numeric value.
\item \code{rounded_latitude} and \code{rounded_longitude}: \code{decimalLatitude} and
\code{decimalLongitude} rounded to \code{precision} decimal places.
}

Any component that ends up as \code{NA} produces an incomplete key.
}

\subsection{Choosing precision}{

GBIF occurrence records often carry coordinates at 5 decimal places (~1 m
at the equator) alongside a \code{coordinateUncertaintyInMeters} field that
typically ranges from tens to hundreds of metres. Rounding coordinates via
\code{precision} lets you define a spatial tolerance appropriate for your data
quality expectations, effectively deciding how close two records must be to
share a key.
}

\subsection{Key classification}{

Keys are considered \strong{complete} when all four components are non-missing.
Keys containing \code{NA} in at least one position (detected by the pattern
\verb{(^|\\\\|)NA(\\\\||$)}) are counted as \strong{incomplete}.
}
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
collection_keys <- get_collections(
  occ_import = occ_import,
  taxa_checked = taxa_checked,
  precision = 2
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=set_vouchers]{set_vouchers()}}, which consumes the output of this function.
}
