% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine_coordinates.R
\name{refine_coordinates}
\alias{refine_coordinates}
\title{Validate coordinates of filtered occurrence records}
\usage{
refine_coordinates(
  custom_filtered = NA,
  threads = 4,
  tests = c("capitals", "centroids", "equal", "gbif", "institutions", "outliers", "seas",
    "zeros")
)
}
\arguments{
\item{custom_filtered}{A \code{customFiltered} object returned by
\code{\link[=custom_filter]{custom_filter()}}.}

\item{threads}{Number of threads to use for coordinate validation, passed to
\code{\link[=set_threads]{set_threads()}}. Use an integer \verb{>= 1} for an absolute count, or a value
between \code{0} and \code{1} for a proportion of available cores. The default is
\code{4}.}

\item{tests}{Character vector of CoordinateCleaner validation tests to
apply. Choose one or more of \code{"capitals"}, \code{"centroids"}, \code{"equal"},
\code{"gbif"}, \code{"institutions"}, \code{"outliers"}, \code{"seas"}, and \code{"zeros"}. The
default uses all tests.}
}
\value{
A \code{CoordinateRefined} object (list) with three elements:
\itemize{
\item \code{CoordinateCleaned}: a \code{data.table} of records that passed all requested
coordinate tests
\item \code{CoordinateProblematic}: a \code{data.table} of records that failed one or more
tests, retaining the CoordinateCleaner flag columns (e.g. \code{.summary}) so
the failing tests can be inspected
\item \code{runtime}: the elapsed execution time
}

Use \code{\link[=detect_native_coord]{detect_native_coord()}} to classify the records with validated
coordinates, and \code{\link[=detect_native_country]{detect_native_country()}} for the coordinate-less records.
}
\description{
Validates the coordinates of filtered occurrence records with
CoordinateCleaner and splits them into coordinate-clean and problematic
tables.

\link[CoordinateCleaner:clean_coordinates]{CoordinateCleaner::clean_coordinates} checks run in parallel to flag
common spatial issues such as centroids, capitals, and marine records.

Records that pass all requested tests are returned in \code{CoordinateCleaned};
records that fail one or more tests are returned in \code{CoordinateProblematic}.
Native-status classification is a separate step: \code{\link[=detect_native_coord]{detect_native_coord()}}
classifies the records with validated coordinates, and
\code{\link[=detect_native_country]{detect_native_country()}} the coordinate-less records (taken directly from
\code{custom_filtered}).
}
\details{
\subsection{Coordinate validation}{

Coordinate validation is performed with CoordinateCleaner. Available tests
are:
\itemize{
\item \code{capitals}: records at country capital coordinates
\item \code{centroids}: records at country or province centroids
\item \code{equal}: records with identical latitude and longitude values
\item \code{gbif}: records matching known GBIF geospatial issues
\item \code{institutions}: records at known herbarium or museum coordinates
\item \code{outliers}: geographic outliers within a species range
\item \code{seas}: records located in marine areas for terrestrial species
\item \code{zeros}: records at coordinates \verb{(0, 0)}
}
}

\subsection{Parallel processing}{

Records with complete coordinates are chunked across the requested number of
workers and validated with \code{foreach} and \code{doParallel}. The worker count is
capped to the number of records to avoid idle cluster nodes.
}

\subsection{Empty input}{

If no records have complete coordinates, validation is skipped and empty
\code{CoordinateCleaned} and \code{CoordinateProblematic} tables are returned.
}
}
\examples{
\dontshow{if (interactive() && exists("filtered")) withAutoprint(\{ # examplesIf}
refined_coordinates(custom_filtered = filtered, threads = 4)
\dontshow{\}) # examplesIf}
}
\references{
\itemize{
\item Zizka, A., Silvestro, D., Andermann, T., Azevedo, J., Duarte Ritter, C., Edler, D., Farooq,
H., Herdean, A., Ariza, M., Scharn, R., Svantesson, S., Wengstrom, N., Vitecek, S., &
Antonelli, A. (2019). CoordinateCleaner: Standardized cleaning of occurrence records from
biological collection databases. \emph{Methods in Ecology and Evolution}, 10(5), 744-751.
\doi{10.1111/2041-210X.13152}
}
}
\seealso{
\code{\link[CoordinateCleaner:clean_coordinates]{clean_coordinates()}},
\code{\link[=custom_filter]{custom_filter()}}, \code{\link[=detect_native_coord]{detect_native_coord()}}, \code{\link[=detect_native_country]{detect_native_country()}},
\code{\link[=export_records]{export_records()}}, \code{\link[=print.CoordinateRefined]{print.CoordinateRefined()}}, \code{\link[=set_threads]{set_threads()}}
}
