% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_issues.R
\name{extract_issues}
\alias{extract_issues}
\title{Count GBIF issue flags per record}
\usage{
extract_issues(occ = NA)
}
\arguments{
\item{occ}{An \code{"import"} \code{data.table} returned by \code{\link[=import_records]{import_records()}},
containing at least the columns \code{gbifID} and \code{issue}. The \code{issue} column
must hold raw GBIF issue codes as present in a GBIF SIMPLE_CSV download.
Defaults to \code{NA}.}
}
\value{
A \code{data.table} with one row per record in \code{occ} and two columns:
\itemize{
\item \code{gbifID}: copied from \code{occ} to allow joining back to the original records.
\item \code{issue_count}: the number of issue codes flagged for that record.
}
}
\description{
Counts, for every occurrence record, how many of the issue codes defined in
\code{\link{EnumOccurrenceIssue}} are flagged in the record's \code{issue} field. This is
the second step in the VasGBIF import workflow, taking the \code{data.table}
returned by \code{\link[=import_records]{import_records()}} as input.

The function is internal: \code{\link[=customized_filter]{customized_filter()}} calls it directly, so users
never invoke it themselves.
}
\details{
The set of recognised issue codes is taken from the package dataset
\code{\link{EnumOccurrenceIssue}}. Each code is searched for in the record's \code{issue}
string, and \code{issue_count} is how many of them are found. Codes not present
in the dataset are ignored.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
gbif_file <- system.file(
  "extdata",
  "0003386-260721160103020.zip",
  package = "VasGBIF"
)
occ <- import_records(path = gbif_file)
head(extract_issues(occ))
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=import_records]{import_records()}} for the preceding step that produces the \code{occ} input.
\item \code{\link[=customized_filter]{customized_filter()}} for the consumer of the issue count.
\item \code{\link{EnumOccurrenceIssue}} for the full list of recognised GBIF issue codes.
}
}
