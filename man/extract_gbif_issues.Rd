% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_issues.R
\name{extract_gbif_issues}
\alias{extract_gbif_issues}
\title{Extract GBIF issue flags into logical columns}
\usage{
extract_gbif_issues(occ = NA)
}
\arguments{
\item{occ}{An \code{"import"} \code{data.table} returned by \code{\link[=import_records]{import_records()}},
containing at least the columns \code{gbifID} and \code{issue}. The \code{issue} column
must hold raw pipe-separated GBIF issue codes as present in a GBIF
SIMPLE_CSV download. Defaults to \code{NA}.}
}
\value{
An object of class \code{"issue"}, implemented as a named list with two elements:
\itemize{
\item \code{occ_issue}: A \code{data.table} with one logical column per issue code in
\code{\link{EnumOccurrenceIssue}}, plus \code{gbifID} for linking to \code{occ} and
\code{issue_count} with the total number of flags per record.
\item \code{summary}: A \code{data.table} with columns \code{issue_keys} and \code{N}, giving the
number of records flagged with each issue code, ordered by decreasing \code{N}.
}
}
\description{
Parses the pipe-separated \code{issue} field from a GBIF occurrence \code{data.table}
and expands it into one logical indicator column per issue code defined in
\code{\link{EnumOccurrenceIssue}}. This is the second step in the VasGBIF import
workflow, taking the \code{data.table} returned by \code{\link[=import_records]{import_records()}} as input.
}
\details{
The set of recognised issue codes is taken from the package dataset
\code{\link{EnumOccurrenceIssue}}. For each code a logical column is created that is
\code{TRUE} when the code appears anywhere in the record's \code{issue} string and
\code{FALSE} otherwise. Codes not present in the dataset are ignored.

Two further columns are appended to the per-code columns:
\itemize{
\item \code{gbifID}: copied from \code{occ} to allow joining back to the original records.
\item \code{issue_count}: the number of issue flags that are \code{TRUE} for each record.
}

The companion \code{summary} table ranks the issue codes by how many records
they flag, which is useful for spotting data-quality problems at a glance.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
gbif_file <- system.file(
  "extdata",
  "0003386-260721160103020.zip",
  package = "VasGBIF"
)
occ <- import_records(path = gbif_file)
gbif_issue <- extract_gbif_issues(occ)
head(gbif_issue$summary, 10)
gbif_issue$occ_issue[, .(gbifID, issue_count)] |> head()
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=import_records]{import_records()}} for the preceding step that produces the \code{occ} input.
\item \code{\link{EnumOccurrenceIssue}} for the full list of recognised GBIF issue codes.
\item \code{\link[=print.issue]{print.issue()}} for a compact summary of the result.
}
}
