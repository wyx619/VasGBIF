% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_vouchers.R
\name{set_vouchers}
\alias{set_vouchers}
\title{Select master digital vouchers from duplicate groups via quality scoring}
\usage{
set_vouchers(occ_import = NA, taxa_checked = NA, collection_keys = NA)
}
\arguments{
\item{occ_import}{An \code{import} object returned by \code{\link[=import_records]{import_records()}}. Used
for the \code{occ_issue} table containing GBIF geospatial issue flags.}

\item{taxa_checked}{An \code{occ_taxa} object returned by \code{\link[=check_taxon]{check_taxon()}}. Used
for the WCVP taxonomic resolution of each record.}

\item{collection_keys}{A \code{collections} object returned by
\code{\link[=get_collections]{get_collections()}}. Used for the \code{collection_key} that groups records
into collection events.}
}
\value{
A list of class \code{"vouchers"} with three elements:
\itemize{
\item \code{occ_digital_voucher}: a \code{data.table} with all occurrence records
and their quality scores, voucher status, grouping flags, taxonomic
assignments, and final classification. Key columns include
\code{moreInformativeRecord}, \code{VasGBIF_digital_voucher},
\code{VasGBIF_duplicates}, \code{VasGBIF_dataset_result}, and
\code{VasGBIF_duplicates_grouping_status}.
\item \code{occ_results}: a \code{data.table} with only the quality-assessment and
result columns, keyed by \code{gbifID}.
\item \code{runtime}: the elapsed execution time.
}
}
\description{
Identifies and selects the best representative record (the
\emph{digital voucher}) from each group of duplicate occurrence records that
share the same collection event key. Records are scored across two quality
dimensions, and the highest-scoring record in each group is retained.
}
\details{
\subsection{How the collection key identifies duplicates}{

The \code{collection_key} from \code{\link[=get_collections]{get_collections()}} has the form
\code{taxon|eventDate|latitude|longitude}. Records that share the \strong{same,
complete} key are considered potential duplicates from a single
gathering event and are processed as a group. Records whose key is
missing or contains \code{NA} in any component cannot be grouped and are
each treated as an independent voucher.
}

\subsection{Quality scoring system}{

Each record receives a total quality score (\code{moreInformativeRecord})
computed as the sum of two sub-scores:

\code{moreInformativeRecord = verbatim_quality + geospatial_quality}
\subsection{\code{verbatim_quality} — record completeness (0–9)}{

One point for each of these nine fields that is present and non-empty:
\code{recordedBy}, \code{recordNumber}, \code{year}, \code{institutionCode},
\code{catalogNumber}, \code{locality}, \code{stateProvince}, \code{countryCode} (via
\code{COUNTRY_INVALID}), and \code{identifiedBy}. A record with all nine fields
scores 9; one with none scores 0.
}

\subsection{\code{geospatial_quality} — coordinate issue penalty (0 to -9)}{

Derived from the GBIF issue flags catalogued in \code{EnumOccurrenceIssue}:
\itemize{
\item \code{0}: no known geospatial issues.
\item \code{-1}: at least one severity-1 (cosmetic) issue, e.g.
\code{COORDINATE_ROUNDED}.
\item \code{-3}: at least one severity-2 (potential) issue, e.g.
\code{COUNTRY_COORDINATE_MISMATCH}.
\item \code{-9}: at least one severity-3 (exclusion) issue, e.g.
\code{ZERO_COORDINATE}, or coordinates are missing entirely.
}
}

\subsection{Worked example}{

Suppose three records share the collection key
\verb{Saxifraga oppositifolia|17095|30.45|79.07}:\tabular{llllllll}{
   Record \tab \code{recordedBy} \tab \code{year} \tab ... \tab \code{verbatim_quality} \tab GBIF issues \tab \code{geospatial_quality} \tab \code{moreInformativeRecord} \cr
   A (complete, no issues) \tab Yes \tab Yes \tab ... \tab 7 \tab none \tab 0 \tab \strong{7} \cr
   B (sparse, minor issue) \tab No \tab No \tab ... \tab 3 \tab \code{COORDINATE_ROUNDED} \tab -1 \tab 2 \cr
   C (missing coordinates) \tab Yes \tab Yes \tab ... \tab 6 \tab \code{ZERO_COORDINATE} \tab -9 \tab -3 \cr
}


Record A has the highest \code{moreInformativeRecord} (7) and becomes the
digital voucher. Its coordinates are propagated to B and C. Records B
and C are classified as \code{"duplicate"}.
}

\subsection{Tie-breaking}{

If two records tie for the highest score, the one appearing first in
the data (smallest row index) is chosen. This is deterministic but
arbitrary; users concerned about a specific tie should inspect those
groups directly.
}

}

\subsection{Voucher selection}{

Within each group, the record with the highest \code{moreInformativeRecord}
is marked \code{VasGBIF_digital_voucher = TRUE}. Its coordinates
(prioritising validated coordinates from the voucher itself) are
assigned to all members of the group via \code{VasGBIF_decimalLatitude}
and \code{VasGBIF_decimalLongitude}.
}

\subsection{Taxonomic consensus}{

For groupable records, the most frequent accepted taxon name (from
WCVP) in the group is assigned to all members. Groups are classified
as:
\itemize{
\item \code{"identified"}: a single accepted name dominates.
\item \code{"divergent identifications"}: multiple accepted names appear.
\item \code{"unidentified"}: no accepted name is present.
}

Non-groupable records take their taxon identity directly from their own
WCVP resolution.
}

\subsection{Final dataset classification}{

\code{VasGBIF_dataset_result} assigns one of three labels:
\itemize{
\item \code{"usable"}: digital voucher, taxonomically identified, and spatially
useful (has validated coordinates).
\item \code{"duplicate"}: not the digital voucher of its group.
\item \code{"unusable"}: digital voucher but either unidentified, lacking
validated coordinates, or both.
}
}
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
taxa_checked <- check_taxon(occ_import = occ_import, accuracy = 0.9)

collection_keys <- get_collections(
  occ_import = occ_import,
  taxa_checked = taxa_checked,
  precision = 2L
)

voucher <- set_vouchers(
  occ_import = occ_import,
  taxa_checked = taxa_checked,
  collection_keys = collection_keys
)
\dontshow{\}) # examplesIf}
}
