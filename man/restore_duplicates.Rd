% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restore_duplicates.R
\name{restore_duplicates}
\alias{restore_duplicates}
\title{Restore missing metadata from duplicate records}
\usage{
restore_duplicates(occ_digital_voucher = NULL)
}
\arguments{
\item{occ_digital_voucher}{A \code{data.table} of digital voucher records, as
produced by internal vouchering steps. Must contain the columns listed in
\code{fields_to_parse} as well as \code{VasGBIF_dataset_result} and
\code{collection_key}.}
}
\value{
A \code{data.table} containing only the usable records, with missing
metadata fields filled from duplicates where possible and a logical flag
\code{VasGBIF_restored_from_duplicate} indicating whether restoration
occurred for each row.
}
\description{
Internal helper that fills missing metadata fields in usable
voucher records using values from their associated duplicate records that
share the same collection event key.
}
\details{
The function operates on two subsets of the input: records marked
\code{"usable"} and those marked \code{"duplicate"}. For each metadata field listed
in \code{fields_to_merge} (\code{eventDate}, \code{year}, \code{month}, \code{day},
\code{identifiedBy}, \code{countryCode}, \code{stateProvince}, \code{locality}), it:
\itemize{
\item Identifies usable records whose value for that field is missing, empty,
or \code{"NA"}.
\item Searches the duplicate records with the same \code{collection_key} for a
non-missing candidate value of reasonable length (no more than 10,000
characters).
\item Copies the first valid candidate into the usable record and sets
\code{VasGBIF_restored_from_duplicate = TRUE}.
}

The \code{year} field receives special treatment: after cleaning, the candidate
text is coerced to integer.

Only the usable records (with restored fields) are returned.
}
\keyword{internal}
